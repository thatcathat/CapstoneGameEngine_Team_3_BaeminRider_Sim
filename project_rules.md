# 프로젝트 규칙 — CGE_T3_BR 배달 라이더 시뮬레이터

이 문서는 현재 저장소의 개발 규칙이다. 이전 캠핑카 프로젝트의 위키 규칙과 전용 시스템은 적용하지 않는다.

## 프로젝트 전제

- Unity 6 프로젝트이며 확인된 에디터 버전은 6000.3.23f1이다. 실제 버전 기준은 `ProjectSettings/ProjectVersion.txt`다.
- URP와 New Input System 패키지가 설치되어 있다. 실제 씬·프로젝트 설정을 확인한 뒤 사용하며 임의로 버전을 변경하지 않는다.
- 게임은 3인칭 3D 로우폴리 배달 시뮬레이터다. PS1 표현, 로컬 LLM, 네트워크 서비스는 필수 요구 사항이 아니다.
- 기획 기준: [배달 라이더 프로토타입 기획서](Docs/배달라이더_프로토타입_기획서.md).
- 기획서의 확정 사항은 유지하고 제안 수치는 테스트 시작값으로 취급한다. 사용자의 새 지시가 있으면 이를 우선한다.

## 1. Unity C# 코딩과 설계

### 책임 분리와 확장성

- 지금 필요한 기능을 가장 단순하게 구현한다. 확장성만을 위해 인터페이스, 기반 클래스, 범용 프레임워크를 미리 만들지 않는다.
- 공통 동작이나 구현 교체가 실제로 필요한 곳에 작은 인터페이스를 사용한다.
- 주문 수락·음식 수령·구매·상환처럼 처리를 요청하고 결과를 확인하는 동작은 명시적인 메서드 호출을 기본으로 한다.
- 배달 완료·현금 변경·하루 종료처럼 여러 기능에 결과를 알릴 때 C# `event` 등을 사용한다. 이벤트 자산은 실제 필요할 때 도입한다.
- 주문 상태 변경과 보상 지급은 명확한 담당 시스템 한 곳에서 처리한다. UI와 효과음 등 구독자는 결과를 표시하며 보상을 중복 지급하지 않는다.
- 이벤트 구독과 해제를 객체의 수명에 맞춰 관리한다.

### 데이터와 실행 상태

- 주문 보상·제한 시간·사건 설정·자전거 가격 등 조정 가능한 설정은 `ScriptableObject` 또는 적절한 직렬화 설정으로 분리한다.
- 설정 자산과 실행 상태를 분리한다. 활성 주문, 음식 보유, 남은 시간, 현금, 빚을 공유 설정 자산의 변경값으로 관리하지 않는다.
- 저장 데이터에는 이어서 플레이하는 데 필요한 진행 상태를 기록한다. 첫 저장 범위는 기획서의 하루 정산 종료 시점이다.
- 기획 변경이 구현에 영향을 주면 관련 기획 내용을 갱신한다. 스크립트별 문서나 일일 작업 로그 생성은 의무가 아니다.

### 성능과 직렬화

- `Update`와 `FixedUpdate`에서 불필요한 검색, 문자열 생성, 반복 할당, 생성·파괴를 피한다. 실제 병목을 확인한 뒤 최적화한다.
- 고정된 컴포넌트 참조는 인스펙터 연결 또는 초기화 시 캐싱한다. 매 프레임 객체 검색으로 연결을 해결하지 않는다.
- `[SerializeField] private` 필드를 기본으로 하고 외부 노출은 필요한 프로퍼티와 메서드로 제한한다.
- 튜닝 수치는 직렬화 설정으로, 의미 있는 고정값은 이름 있는 상수로 표현한다. 명확한 모든 숫자까지 기계적으로 상수화하지 않는다.
- 직렬화된 필드 이름이나 타입 변경 시 기존 저장값과 인스펙터 연결을 보존하는 마이그레이션을 적용하고 확인한다.
- 물리 조작과 카메라 갱신은 각 기능에 적절한 Unity 실행 주기를 사용한다.

### 입력과 비동기 처리

- New Input System을 사용하고 플레이, 지도·주문 UI, 정산, 일시정지에서 입력이 충돌하지 않도록 분리한다.
- UI에 입력을 넘기는 동작과 게임 시간을 멈추는 동작을 구분한다. 지도는 시간이 흐르고 일시정지는 게임과 타이머가 함께 멈춘다.
- 실제 필요한 로딩·파일 입출력 등에만 비동기를 사용한다. Unity API의 메인 스레드 요구 사항을 지킨다.
- 장시간 작업에는 취소와 예외 처리를 두고 씬 종료·객체 파괴 후 결과가 적용되지 않게 한다.
- 별도 비동기 라이브러리는 필요성이 확인되지 않으면 추가하지 않는다.

## 2. 배달 게임의 핵심 동작 보장

- 플레이어는 도보로 시작해 자전거를 구매한다. 자전거가 없어도 모든 주문 목적지에 접근할 수 있어야 한다.
- 주문은 한 번에 한 건이며 수락 순간 주문 제한 시간이 시작된다. 지각해도 주문을 유지하고 지각 보상을 적용한다.
- 주문 포기는 제공하지 않는다. 음식 도난과 접근 불가능 상태에는 반드시 복구 경로를 둔다.
- 해당 주문의 음식을 보유하고 해당 고객과 충돌했을 때만 전달한다. 일반 주민 충돌과 음식 없는 충돌은 완료가 아니다.
- 한 주문의 완료·기본 보상·팁은 각각 중복 적용되지 않아야 한다.
- 하루 종료 시 신규 주문 수락을 막고 현재 주문의 수령·복구·전달을 끝낸 뒤 한 번만 정산한다.
- 음식 도난은 상황에 따라 추격 회수 또는 재수령으로 해결한다. 추격 대상을 놓쳐도 복구가 가능해야 한다.
- 관리비 부족 시 현금은 0원이 되고 부족분만 빚에 추가한다. 상환과 구매로 현금이 음수가 되지 않게 한다.
- 빚 전액 상환 시 엔딩으로 진행한다.

## 3. 프로젝트 파일과 에셋

### 폴더 구조

새 게임 전용 자산은 아래 구조를 기본으로 하되 필요한 폴더부터 생성한다. 규칙을 맞추기 위한 기존 자산의 일괄 이동은 하지 않는다.

```text
Assets/_Game/
  Scripts/
    Core/          하루 흐름, 시간, 저장
    Player/        도보·자전거 이동, 카메라
    Delivery/      주문, 수령, 전달, 요청 사항
    Events/        사고, 도난, 복구
    NPC/           주민, 고객, 도둑
    Economy/       정산, 관리비, 빚, 구매
    UI/            HUD, 지도, 정산 화면
    Data/          설정 데이터 타입
  Editor/          에디터 전용 자동화
  Scenes/
  Prefabs/
  Settings/        설정 자산
  Art/
  Audio/
Docs/              기획과 필요한 운영 안내
```

### 파일 안전과 자동화

- `.meta`와 GUID를 임의로 생성·수정·재생성하지 않는다. Unity가 생성하도록 하고 에셋과 대응 `.meta`를 함께 버전 관리한다.
- 에셋 이동·삭제는 Unity 에디터 또는 에디터 API로 처리해 참조를 보존한다.
- `.unity`, `.prefab` 등 Unity 직렬화 자산은 텍스트를 직접 수정하지 않고 에디터 또는 에디터 API로 변경한다.
- 씬 구성과 컴포넌트 연결은 가능한 경우 연결된 에디터 도구나 에디터 스크립트로 자동화한다. 반복 실행 시 중복 생성되지 않게 설계한다.
- 사용자에게 수동 조작이 필요한 경우 정확한 메뉴와 절차를 안내한다. 에디터 실행을 확인하지 않았다면 완료했다고 보고하지 않는다.
- 패키지 설치·업그레이드는 작업에 필요한 범위로 제한한다. 외부 에셋과 기존 사용자 작업물을 임의로 수정하지 않는다.
- 생성 캐시와 임시 파일은 작업 결과물로 추가하지 않는다.

## 4. 워크플로우와 검증

- 확정 기획 구현, 국소적 리팩토링, 버그 수정은 요청받은 범위에서 진행하고 결과를 보고한다.
- 큰 구조 변경은 목적과 영향을 먼저 설명한다. 이미 승인된 작업 범위의 구현 선택 때문에 반복 승인을 요청하지 않는다.
- 핵심 게임 규칙 변경, 사용자 작업물 삭제, 되돌리기 어려운 변경이 기존 요청에 포함되지 않았다면 실행 전에 확인한다.
- 기존 동작 코드의 삭제·대체가 필요하면 이유와 영향을 설명한다.
- 오류는 메시지·관련 코드·재현 조건을 확인하고 근본 원인을 최소한의 적절한 변경으로 수정한다. 근거 없이 전체 코드를 교체하지 않는다.
- 코드 변경 후 코드 검토와 필요한 최소 컴파일 오류 확인까지만 수행한다. 반복 로그 조회를 피하고 확인하지 않은 내용은 명확히 보고한다.
- 실제 플레이 검증은 사용자가 직접 수행한다. 별도 요청이 없으면 Play 모드 진입, 자동 입력·플레이 테스트, 화면 캡처 반복, 플레이 검증용 코드 추가를 하지 않는다. 씬 제작·편집·저장에 필요한 에디터 연결은 계속 사용한다.
- 사용자가 테스트를 명시적으로 요청한 경우에만 해당 범위의 테스트를 실행한다. 평소에는 사용자가 확인할 짧은 조작 절차와 핵심 확인 항목을 제공한다.
- 검증 수준은 구분한다: 파일 작성, 컴파일 통과, 에디터 실행, 플레이 검증은 서로 다른 완료 상태다.

## 5. 응답 스타일

- 한국어로 설명하고 C# 식별자는 영어를 사용한다. 타입·메서드·프로퍼티는 PascalCase, 지역 변수·매개변수는 camelCase를 기본으로 하며 기존 파일의 일관성을 존중한다.
- 작업 결과는 기본적으로 무엇을 변경했는지, 왜 변경했는지, 무엇을 검증했는지를 3줄 안팎으로 요약한다.
- 복잡한 변경, 중요한 제한, 사용자의 수동 조작 절차는 필요한 만큼 설명한다.
- 실제 선택이 필요한 대안에 대해서만 장단점과 추천안을 제시한다.

## 6. 기본 개발 철학

모든 구현과 리팩토링에서 다음 순서로 검토하고 가장 단순한 적절한 단계에서 멈춘다.

1. YAGNI: 지금 필요한 기능인가? 필요 없으면 만들지 않는다.
2. 재사용: 기존 코드와 자산으로 해결할 수 있는가?
3. 표준 기능: C# / .NET 표준 기능으로 적절히 해결할 수 있는가?
4. 엔진 기능: Unity, URP, New Input System의 기능으로 해결할 수 있는가?
5. 최소 동작 코드: 현재 요구를 충족하는 간결하고 명확한 코드를 작성한다.

표준 기능과 엔진 기능은 작업에 적합한 쪽을 선택한다. 버그의 근본 원인은 수정하되, 이를 이유로 관련 없는 구조까지 확대 개편하지 않는다.
